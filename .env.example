# ── Azure Atlas — root env vars ──────────────────────────────
# Copy to .env and adjust values as needed.
#   cp .env.example .env

# API key for write operations (curation, events).
# Leave empty for development (no auth enforced).
ATLAS_API_KEY=
